import(tcltk)

export(getSocketClients,
       getSocketClientsNames,
       getSocketServerName,
       getSocketServers,
	   parSocket,
       processSocket,
       sendSocketClients,
	   sendSocketServer,
       closeSocketClients,
       startSocketServer,
       stopSocketServer)
